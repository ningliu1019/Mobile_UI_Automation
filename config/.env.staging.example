# Copy this file to .env.staging and fill in real values.
# .env.staging is gitignored — never commit real credentials.

TWITCH_STAGING_USERNAME=your_staging_username
TWITCH_STAGING_PASSWORD=your_staging_password
